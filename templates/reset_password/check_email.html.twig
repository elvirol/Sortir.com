{% extends 'base.html.twig' %}

{% block title %}Courriel de réinitialisation du mot de passe envoyé{% endblock %}

{% block body %}
    <div class="min-h-screen flex items-center justify-center ">
        <div class="w-full max-w-lg bg-white shadow-md rounded-lg p-6">
            <h1 class="text-2xl font-bold text-gray-800 text-center mb-6">Courriel de réinitialisation envoyé</h1>

            <p class="text-gray-700 mb-4">
                Si un compte correspondant à votre adresse e-mail existe, un courriel contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe vient d'être envoyé.
                Ce lien expire dans
                <span class="font-semibold text-gray-800">{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</span>.
            </p>

            <p class="text-gray-700">
                Si vous ne recevez pas le courriel, veuillez vérifier votre dossier spam ou
                <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:underline">réessayer</a>.
            </p>
        </div>
    </div>
{% endblock %}